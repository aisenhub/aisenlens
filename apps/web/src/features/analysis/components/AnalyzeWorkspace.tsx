import { memo, type ReactNode } from "react"
import { Button } from "../../../components/ui/button"
import type { ShotData } from "../../editor/constants/editorData.ts"
import type { ShotGroupRecord } from "../../group/types"
import ShotBrowserView from "../../shot/components/ShotBrowserView"
import ContextInspector from "./ContextInspector"
import SoundWorkspace from "../../media/components/SoundWorkspace"
import type { ProjectRecord } from "../../project/types"
import type { WorkflowView } from "../../workflow/types.ts"
import { useMemo } from "react"
import { useProjectSession } from "../../editor/session/ProjectSessionProvider"
import type { EvidenceRef, ResearchContext, ResearchRange, ResearchTarget } from "../types.ts"
import RangeInspector from "./RangeInspector"

interface AnalyzeWorkspaceProps { view: WorkflowView; children: ReactNode; shots: ShotData[]; groups: ShotGroupRecord[]; activeShotIndex: number; selectedGroupId?: string | null; notes: Record<string, { content: string; analysis: string }>; project: ProjectRecord; frameRate: number; currentFrame: number; currentTime: number; mediaIdentityDigest: string; researchTarget: ResearchTarget | null; researchRanges: ResearchRange[]; researchContexts: ResearchContext[]; firstScreenshotId?: string | null; lastScreenshotId?: string | null; onViewChange: (view: "scenes" | "shots" | "sound") => void; onLocateShot: (index: number) => void; onPlayShot: (index: number) => void; onChangeNotes: (patch: { content?: string; analysis?: string }) => void; onUpdateResearchContext: (target: ResearchTarget, patch: Partial<Pick<ResearchContext, "question" | "status" | "needsReview">>) => Promise<void>; onAddEvidence: (target: ResearchTarget, evidence: EvidenceRef) => void; onRemoveEvidence: (target: ResearchTarget, evidenceId: string) => void; onCreateResearchRange: (startUs: number, endUs: number) => Promise<ResearchRange | null>; onUpdateResearchRange: (range: ResearchRange, patch: Partial<Pick<ResearchRange, "startUs" | "endUs" | "title" | "observation" | "interpretation" | "summary">>) => Promise<void>; onProjectUpdated: (project: ProjectRecord) => void; onSaveAndNext?: () => Promise<void>; isResearchSaving?: boolean; researchError?: string | null }

function AnalyzeWorkspace({ view, children, shots, groups, activeShotIndex, selectedGroupId = null, notes, project, frameRate, currentFrame, currentTime, mediaIdentityDigest, researchTarget, researchRanges, researchContexts, firstScreenshotId, lastScreenshotId, onViewChange, onLocateShot, onPlayShot, onChangeNotes, onUpdateResearchContext, onAddEvidence, onRemoveEvidence, onCreateResearchRange, onUpdateResearchRange, onProjectUpdated, onSaveAndNext, isResearchSaving = false, researchError = null }: AnalyzeWorkspaceProps) {
  const researchMode = useProjectSession((state) => state.researchMode)
  const researchScope = useProjectSession((state) => state.researchScope)
  const setResearchMode = useProjectSession((state) => state.setResearchMode)
  const setResearchScope = useProjectSession((state) => state.setResearchScope)
  const sessionTargetValue = useProjectSession((state) => state.researchTarget)
  const sessionTarget: ResearchTarget | null = sessionTargetValue && (sessionTargetValue.kind === "shot" || sessionTargetValue.kind === "group" || sessionTargetValue.kind === "range") ? { kind: sessionTargetValue.kind, id: sessionTargetValue.id } : null
  const activeShot = shots[activeShotIndex] ?? null
  const activeGroup = selectedGroupId ? groups.find((group) => group.id === selectedGroupId) ?? null : null
  const target = researchTarget ?? sessionTarget ?? (activeGroup ? { kind: "group" as const, id: activeGroup.id } : activeShot ? { kind: "shot" as const, id: activeShot.id } : null)
  const targetContext = target ? researchContexts.find((context) => context.target.kind === target.kind && context.target.id === target.id) ?? null : null
  const activeRange = target?.kind === "range" ? researchRanges.find((range) => range.id === target.id) ?? null : null
  const scopeShotIds = useMemo(() => {
    if (researchMode !== "range") return undefined
    if (researchScope.kind === "group" && researchScope.id) return groups.find((group) => group.id === researchScope.id)?.shotIds ?? []
    if (researchScope.fromUs === undefined || researchScope.toUs === undefined) return undefined
    return shots.filter((shot) => shot.start * 1_000_000 < researchScope.toUs! && (shot.start + shot.duration) * 1_000_000 > researchScope.fromUs!).map((shot) => shot.id)
  }, [groups, researchMode, researchScope, shots])
  const currentView = view === "shots" || view === "sound" ? view : "scenes"
  return <div className="flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden bg-bg"><div className="flex shrink-0 flex-wrap items-center gap-1 border-b border-border bg-bg-nav px-3 py-2" role="tablist" aria-label="深拆视图"><span className="mr-3 shrink-0 font-mono text-[10px] uppercase tracking-wider text-text-muted">Analyze</span>{(["scenes", "shots", "sound"] as const).map((item) => <Button key={item} type="button" variant="ghost" size="sm" role="tab" aria-selected={currentView === item} onClick={() => onViewChange(item)} className={currentView === item ? "shrink-0 bg-accent/15 text-accent" : "shrink-0 text-text-muted"}>{item === "scenes" ? "Scenes" : item === "shots" ? "Shots" : "Sound"}</Button>)}<span className="mx-1 hidden h-4 w-px bg-border sm:block" aria-hidden="true" /><span className="text-xs text-text-muted">研究模式</span><Button type="button" variant="ghost" size="sm" aria-pressed={researchMode === "sequential"} onClick={() => { setResearchMode("sequential"); setResearchScope({ kind: "full-film" }) }} className={researchMode === "sequential" ? "bg-accent/15 text-accent" : "text-text-muted"}>逐镜</Button><Button type="button" variant="ghost" size="sm" aria-pressed={researchMode === "range"} onClick={() => setResearchMode("range")} className={researchMode === "range" ? "bg-accent/15 text-accent" : "text-text-muted"}>选段</Button>{researchMode === "range" && <span className="ml-auto text-[11px] text-text-muted">{scopeShotIds ? `${scopeShotIds.length} 镜在当前范围` : "当前为媒体时间范围"}</span>}{isResearchSaving && <span className="text-[11px] text-accent">研究保存中…</span>}{researchError && <span role="alert" className="max-w-64 truncate text-[11px] text-red-300" title={researchError}>研究保存失败：{researchError}</span>}</div><div className={currentView === "scenes" ? "flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden" : "hidden"}>{children}</div>{currentView === "shots" && <div className="flex min-h-0 min-w-0 flex-1 flex-col overflow-hidden lg:flex-row"><ShotBrowserView shots={shots} activeShotIndex={activeShotIndex} scopeShotIds={scopeShotIds} notes={notes} onLocateShot={onLocateShot} onPlayShot={onPlayShot} onSaveAndNext={onSaveAndNext} queuePosition={scopeShotIds ? Math.max(0, scopeShotIds.indexOf(activeShot?.id ?? "")) : activeShotIndex} queueLength={scopeShotIds?.length ?? shots.length} isResearchSaving={isResearchSaving} /><>{target?.kind === "range" ? <RangeInspector range={activeRange} context={targetContext} mediaIdentityDigest={mediaIdentityDigest} currentTime={currentTime} onCreate={async (startUs, endUs) => onCreateResearchRange(startUs, endUs)} onUpdateRange={onUpdateResearchRange} onUpdateContext={(patch) => onUpdateResearchContext(target, patch)} onAddEvidence={(evidence) => onAddEvidence(target, evidence)} onRemoveEvidence={(evidenceId) => onRemoveEvidence(target, evidenceId)} /> : <ContextInspector projectId={project.id} targetKind={activeGroup ? "group" : "shot"} target={target} context={targetContext} mediaIdentityDigest={mediaIdentityDigest} currentTime={currentTime} firstScreenshotId={firstScreenshotId} lastScreenshotId={lastScreenshotId} className="basis-1/2 overflow-y-auto border-t border-border lg:w-80 lg:flex-none lg:basis-auto lg:overflow-hidden lg:border-t-0" shot={activeShot} shotIndex={activeShotIndex} notes={notes[activeShot?.id ?? ""] ?? { content: "", analysis: "" }} group={activeGroup} onChangeNotes={onChangeNotes} onUpdateContext={(patch) => target ? onUpdateResearchContext(target, patch) : Promise.resolve()} onAddEvidence={(evidence) => target && onAddEvidence(target, evidence)} onRemoveEvidence={(evidenceId) => target && onRemoveEvidence(target, evidenceId)} />}</></div>}{currentView === "sound" && <SoundWorkspace project={project} frameRate={frameRate} currentFrame={currentFrame} currentTime={currentTime} mediaIdentityDigest={mediaIdentityDigest} researchTarget={target} researchRange={activeRange} researchContext={targetContext} onCreateResearchRange={onCreateResearchRange} onUpdateResearchRange={onUpdateResearchRange} onUpdateResearchContext={onUpdateResearchContext} onAddEvidence={(evidence) => { if (target) onAddEvidence(target, evidence) }} onRemoveEvidence={(evidenceId) => { if (target) onRemoveEvidence(target, evidenceId) }} onProjectUpdated={onProjectUpdated} />}</div>
}

const areAnalyzeWorkspacePropsEqual = (previous: AnalyzeWorkspaceProps, next: AnalyzeWorkspaceProps) => {
  if (previous.view !== next.view) return false
  if (next.view === "shots") {
    return previous.shots === next.shots && previous.groups === next.groups && previous.activeShotIndex === next.activeShotIndex && previous.selectedGroupId === next.selectedGroupId && previous.notes === next.notes && previous.researchTarget === next.researchTarget && previous.researchRanges === next.researchRanges && previous.researchContexts === next.researchContexts && previous.currentTime === next.currentTime && previous.researchError === next.researchError && previous.isResearchSaving === next.isResearchSaving
  }
  if (next.view === "sound") {
    return previous.project === next.project && previous.frameRate === next.frameRate && previous.currentFrame === next.currentFrame && previous.currentTime === next.currentTime && previous.researchTarget === next.researchTarget && previous.researchRanges === next.researchRanges && previous.researchContexts === next.researchContexts
  }
  return false
}

export default memo(AnalyzeWorkspace, areAnalyzeWorkspacePropsEqual)
