- # Project Overview

  This is a React + Vite + Tailwind CSS application.

  The project was originally generated from Figma Make, but development is now fully independent from Figma Make.

  ---

   ## UI 组件复用规则

    实现任何 UI 前，先按以下顺序检查：

    1. 检查 `src/components/ui/` 是否已有可复用的 shadcn/ui 组件。
    2. 检查同一 feature 或相邻页面是否已有可复用的业务组件。
    3. 若现有组件只差少量样式或行为，优先扩展或传入 `className`，不要复制实现。
    4. 仅当现有组件无法满足需求时，才执行：
       `pnpm dlx shadcn@latest add <组件名> --yes`
    5. 新增 shadcn 组件必须放在 `src/components/ui/`，并匹配 `src/index.css` 的 AisenLens 主题 token。
    6. 通用交互优先使用 Button、Dialog、DropdownMenu、Tooltip、Tabs 和 Sonner 通知；业务逻辑不得放入通用 UI 组件。
    8. 每次引入或修改组件后，运行 `pnpm.cmd build` 验证。
  
  
  
  ⸻
  
  Initial Project Analysis and Refactoring
  
  When first accessing this project, do not immediately implement new features.
  
  The project was originally generated by Figma Make.
  
  The first task is to analyze and reorganize the existing codebase into the defined architecture.
  
  ⸻
  
  Phase 1: Codebase Analysis
  
  Before modifying code:
  
  1. Analyze the current project structure.
  2. Identify existing components.
  3. Identify duplicated code.
  4. Identify large files that need separation.
  5. Understand current application flow.
  
  Do not delete existing functionality.
  
  Do not rewrite the application from scratch.
  
  ⸻
  
  Phase 2: Architecture Migration
  
  Gradually migrate the existing code into the following structure:
  
  src/
  ├── app/
  ├── pages/
  ├── components/
  ├── features/
  ├── hooks/
  ├── stores/
  ├── services/
  ├── database/
  ├── types/
  ├── utils/
  ├── constants/
  ├── assets/
  └── main.tsx
  
  ⸻
  
  App.tsx Refactoring Rules
  
  The existing App.tsx may contain too much code.
  
  Refactor App.tsx first.
  
  After refactoring:
  
  App.tsx should only contain:
  
  * Application layout
  * Router
  * Global providers
  * Application initialization
  
  App.tsx should NOT contain:
  
  * Video player logic
  * Timeline logic
  * Database queries
  * Authentication logic
  * AI requests
  * Complex UI components
  
  Example:
  
  Before:
  
  App.tsx
  ├── Header
  ├── Sidebar
  ├── Video Player
  ├── Timeline
  ├── Analysis Panel
  ├── Supabase Query
  ├── User Logic
  └── AI Request
  
  After:
  
  App.tsx
  ├── Router
  ├── Providers
  └── Layout
  
  ⸻
  
  Component Separation Rules
  
  When a component becomes too large:
  
  Split it according to responsibility.
  
  Example:
  
  Before:
  
  VideoPage.tsx
  1000 lines
  
  After:
  
  features/video/
  ├── components/
  │   ├── VideoPlayer.tsx
  │   ├── VideoControls.tsx
  │   └── VideoTimeline.tsx
  ├── hooks/
  │   └── useVideo.ts
  └── services/
      └── videoService.ts
  
  ⸻
  
  Figma Generated Code Migration Rules
  
  The project contains Figma-generated components.
  
  Do not remove them immediately.
  
  Classify existing code:
  
  Reusable UI
  
  Move to:
  
  src/components/
  
  Examples:
  
  * Button
  * Modal
  * Input
  * Card
  * Layout components
  
  Business Features
  
  Move to:
  
  src/features/
  
  Examples:
  
  * Video analysis
  * Timeline
  * User projects
  * Authentication
  
  Static Resources
  
  Keep in:
  
  src/assets/
  
  Examples:
  
  * Logo
  * Images
  * Icons
  
  ⸻
  
  Refactoring Priority
  
  Follow this order:
  
  Step 1
  
  Analyze existing files.
  
  Do not change functionality.
  
  ⸻
  
  Step 2
  
  Separate App.tsx.
  
  Move:
  
  * Pages
  * Components
  * Business logic
  
  into correct folders.
  
  ⸻
  
  Step 3
  
  Create feature modules.
  
  Move:
  
  * Video related code → features/video
  * User related code → features/auth
  * Project related code → features/project
  
  ⸻
  
  Step 4
  
  Create service layer.
  
  Move:
  
  * Supabase operations
  * API requests
  
  into:
  
  src/services/
  
  ⸻
  
  Step 5
  
  Verify application.
  
  After each migration:
  
  * Ensure project builds successfully.
  * Ensure UI remains unchanged.
  * Fix import paths.
  * Avoid breaking existing features.
  
  ⸻
  
  Refactoring Restrictions
  
  During restructuring:
  
  Do not:
  
  * Change UI design.
  * Add new features.
  * Replace technology stack.
  * Introduce unnecessary dependencies.
  * Rewrite the entire application.
  
  The goal is:
  
  “Same application behavior, cleaner architecture.”
  
  ⸻
  
  Development Workflow After Refactoring
  
  Only after the architecture migration is complete:
  
  Start implementing new features.
  
  All new features must follow the defined project structure.
  
  ⸻
  
  
  
  
  
  
  
  
  
  
  
  ------
  
  
  
  # Development Environment
  
  ## Tech Stack
  
  - Framework: React 19
  - Build Tool: Vite
  - Language: TypeScript
  - Styling: Tailwind CSS v4
  - Package Manager: pnpm
  - State Management: Zustand
  
  Use the existing development workflow:
  
  - Start development server with the existing pnpm scripts.
  - Do not introduce alternative build systems.
  - Do not migrate frameworks.
  - Use Zustand for global client state. Do not store complex application state only inside React components.


---

  # Project Structure

  Project Structure

  Follow the existing project structure.

  The application uses a scalable React + Supabase architecture.

  Do not create random folders.

  Do not put business logic into UI components.

  Do not create unnecessary files or folders in the root directory.

  Keep the root directory clean and organized.

```
project-root/

├── src/
│
├── public/
│
├── supabase/
│
├── docs/
│
├── reference-projects
│
├── tests/
│
├── scripts/
│
├── .env
├── .env.example
├── .gitignore
├── package.json
├── pnpm-lock.yaml
├── vite.config.ts
├── tsconfig.json
├── index.html
├── README.md
```

Project Root Structure

The root directory contains application code, backend configuration, documentation, and development tools.

⸻

src/

Frontend application source code.

Contains:

* React components
* Pages
* Features
* Business logic
* Frontend services

Rules:

* All frontend code must be inside src/.
* Do not place React code outside src/.
* Do not store configuration files here.

⸻

public/

Static files served directly by the web server.

Contains:

* favicon
* robots.txt
* web manifest
* public static files

Examples:

public/
├── favicon.ico
├── robots.txt
└── manifest.json

Rules:

* Files in public are not imported into React.
* Do not store application images here.
* Do not store user uploaded files here.
* Do not store videos here.

⸻

supabase/

Supabase backend configuration.

Contains:

* Database migrations
* Edge Functions
* Supabase settings

Structure:

supabase/
├── migrations/
├── functions/
└── config.toml

Rules:

* All backend-related Supabase code belongs here.
* Do not put Supabase server logic inside React components.

⸻

supabase/migrations/

Database migration files.

Contains:

* Table creation
* Table changes
* Database policies
* Index creation

Examples:

migrations/
001_create_profiles.sql
002_create_projects.sql
003_create_video_tables.sql

Rules:

* Every database structure change must have a migration file.
* Do not manually modify database structure without migration.

⸻

supabase/functions/

Server-side functions.

Used for operations that cannot run safely in the browser.

Contains:

* AI API calls
* Secure backend logic
* Video processing tasks
* Third-party API integrations

Examples:

functions/
├── analyze-video/
├── generate-report/
└── process-video/

Rules:

* Never expose private API keys in frontend code.
* Sensitive operations must run here.
* Keep functions independent and focused.

⸻

docs/

Project documentation.

Contains:

* Product requirements
* Technical architecture
* Database design
* Development notes

Examples:

docs/
├── product.md
├── architecture.md
└── database.md

Rules:

* Record important technical decisions here.
* Keep documentation updated when architecture changes.

⸻

tests/

Automated testing files.

Contains:

* Unit tests
* Component tests
* Integration tests

Examples:

tests/
├── components/
├── features/
└── services/

Rules:

* Tests should not contain production code.
* Keep test files separated from application files.

⸻

scripts/

Development automation scripts.

Contains:

* Data migration scripts
* Code generation scripts
* Development utilities

Examples:

scripts/
├── generate-types.ts
└── migrate-data.ts

Rules:

* Scripts are for development tools only.
* Do not put application business logic here.

⸻

.env

Environment configuration file.

Contains:

* Supabase URL
* Public keys
* Environment variables

Example:

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

Rules:

* Never commit .env to Git.
* Never store sensitive secrets in frontend variables.
* Use .env.example as a template.

⸻

.env.example

Environment variable template.

Contains:

* Required environment variable names
* Example values

Purpose:

* Help developers configure the project.
* Document required environment settings.

Rules:

* Do not put real secrets here.

⸻

package.json

Project dependency and script configuration.

Contains:

* Dependencies
* Development scripts
* Project metadata

Rules:

* Use pnpm as package manager.
* Do not manually modify dependencies without checking existing packages.

⸻

pnpm-lock.yaml

Dependency lock file.

Purpose:

* Ensure consistent dependency versions.
* Keep development and production environments synchronized.

Rules:

* Always commit this file.
* Do not manually edit this file.

⸻

vite.config.ts

Vite build configuration.

Contains:

* React plugin configuration
* Build settings
* Path aliases

Rules:

* Avoid unnecessary modifications.
* Do not introduce another build system.

⸻

tsconfig.json

TypeScript configuration.

Controls:

* Type checking
* Module resolution
* Compiler options

Rules:

* Maintain strict type safety.
* Avoid disabling TypeScript checks without reason.

⸻

index.html

Application HTML entry file.

Contains:

* Root DOM element
* Page metadata
* Loading configuration

Rules:

* Keep this file simple.
* Do not place application logic here.

⸻

README.md

Project introduction and development instructions.

Contains:

* Project overview
* Installation steps
* Development commands
* Deployment instructions

Rules:

* Keep updated when project setup changes.

⸻

Root Directory Rules

When adding new files:

1. First check if an existing directory is suitable.
2. Avoid creating new root folders without a clear purpose.
3. Keep frontend code inside src/.
4. Keep Supabase backend code inside supabase/.
5. Keep documentation inside docs/.
6. Keep development tools inside scripts/.

The goal is a clean, scalable production application structure.











```
src/
├── app/
├── pages/
├── components/
├── features/
├── hooks/
├── stores/
├── services/
├── database/
├── types/
├── utils/
├── constants/
├── assets/
│
└── main.tsx
```

  ⸻

  src/main.tsx

  * React application entry point.
  * Creates the React root.
  * Mounts the App component.

  Rules:

  * Do not add business logic here.
  * Only handle application initialization.

  ⸻

  src/app/

  Application initialization layer.

  Contains:

  * App.tsx
  * Router configuration
  * Global providers

  Responsibilities:

  * Application startup
  * Route management
  * Global configuration

  Rules:

  * App.tsx should only organize application structure.
  * Do not put complex business logic here.
  * Do not put video processing code here.
  * Do not put database queries here.

  Avoid creating a huge App.tsx file.

  ⸻

  src/pages/

  Page-level components.

  Each folder represents a complete application page.

  Examples:

  * Login page
  * Dashboard page
  * Project page
  * Settings page

  Responsibilities:

  * Combine multiple components.
  * Control page layout.
  * Connect different features.

  Rules:

  * Pages should not contain complex business logic.
  * Pages should call feature modules.
  * Keep pages focused on layout and composition.

  ⸻

  src/components/

  Reusable UI components.

  Contains:

  * Buttons
  * Dialogs
  * Forms
  * Layout components
  * Common interface elements

  Example:

  components/
  ui/
  Button
  Modal
  Input
  layout/
  Header
  Sidebar
  Container

  Rules:

  * Components should be reusable.
  * Do not contain business logic.
  * Do not directly access Supabase.
  * Do not directly modify global data.

  ⸻

  src/features/

  Core business modules.

  Each product feature has its own folder.

  Examples:

  features/
  auth/
  video/
  timeline/
  annotation/
  analysis/
  project/

  Responsibilities:

  * Business logic
  * Feature components
  * Feature hooks
  * Feature services
  * Feature types

  Rules:

  * New product features must be created inside features.
  * Do not put feature logic inside components.
  * Keep each feature independent.

  ⸻

  src/features/auth/

  User authentication module.

  Contains:

  * Login
  * Register
  * User session
  * Permission management

  Handles:

  * Supabase Auth
  * User state
  * Authentication flow

  Rules:

  * Never store passwords manually.
  * Never implement custom authentication.
  * Use Supabase Auth.

  ⸻

  src/features/video/

  Video management module.

  Contains:

  * Video player
  * Video upload
  * Video metadata
  * Frame processing

  Handles:

  * Video playback
  * Video information
  * Video operations

  Rules:

  * Video logic must stay inside this module.
  * Do not put video processing code in App.tsx.

  ⸻

  src/features/timeline/

  Timeline editing module.

  Contains:

  * Timeline component
  * Progress control
  * Frame navigation
  * Markers

  Handles:

  * Video timeline interaction.
  * Current playback position.
  * Frame selection.

  Rules:

  * Timeline state should not be scattered across components.

  ⸻

  src/features/annotation/

  Video annotation module.

  Contains:

  * Notes
  * Markers
  * Comments
  * Labels

  Handles:

  * User notes
  * Scene annotations
  * Analysis marks

  ⸻

  src/features/analysis/

  AI analysis module.

  Contains:

  * AI requests
  * Analysis results
  * Analysis display

  Handles:

  * Scene analysis
  * Composition analysis
  * AI suggestions

  Rules:

  * AI API calls must not be written inside UI components.

  ⸻

  src/features/project/

  User project management module.

  Contains:

  * Create project
  * Save project
  * Load project
  * Project settings

  Handles:

  * User video projects.
  * Project data synchronization.

  ⸻

  src/hooks/

  Global reusable React hooks.

  Examples:

  * useDebounce
  * useMediaQuery
  * useLocalStorage

  Rules:

  * Only store reusable hooks.
  * Feature-specific hooks belong inside features.

  Example:

  Correct:

  features/video/hooks/useVideo.ts

  Wrong:

  hooks/useVideo.ts

  ⸻

  src/stores/

  Global client state management.

  Uses:

  * Zustand

  Stores:

  * User state
  * Current project
  * Video state
  * UI preferences

  Rules:

  * Only store client-side state.
  * Do not replace database with Zustand.
  * Do not store large data here.

  ⸻

  src/services/

  External service communication layer.

  Contains:

  * Supabase connection
  * Database operations
  * Storage operations
  * API requests

  Example:

  services/
  supabase/
  auth/
  storage/

  Rules:

  * Components cannot directly call Supabase.
  * All external communication goes through services.

  ⸻

  src/database/

  Database related files.

  Contains:

  * Supabase generated types
  * Database models
  * Table definitions

  Rules:

  * Keep database types synchronized with Supabase.
  * Do not duplicate database structures.

  ⸻

  src/types/

  Global TypeScript types.

  Contains:

  * User types
  * Project types
  * Common interfaces

  Rules:

  * Only shared types.
  * Feature-specific types stay inside features.

  ⸻

  src/utils/

  Pure utility functions.

  Examples:

  * Format time
  * Calculate frame number
  * File size conversion

  Rules:

  * No React components.
  * No database access.
  * No business logic.

  ⸻

  src/constants/

  Global constants.

  Examples:

  * Routes
  * Configuration
  * Application limits

  Rules:

  * Keep fixed values here.
  * Avoid hardcoding values inside components.

  ⸻

  src/assets/

  Static resources.

  Contains:

  * Images
  * Icons
  * Fonts

  Rules:

  * Only store application static files.
  * Do not store user uploaded videos here.

  ⸻

  src/styles/

  Global styling files.

  Contains:

  * Global CSS
  * Theme settings
  * Tailwind customization

  Rules:

  * Avoid creating many CSS files.
  * Prefer Tailwind utility classes.

  ⸻

  Development Rules

  When adding a new feature:

  1. First identify the correct feature module.
  2. Keep UI and business logic separated.
  3. Keep database operations inside services.
  4. Keep reusable components inside components.
  5. Avoid modifying App.tsx unless necessary.

  The goal is a maintainable production-level application.

  

---

  # AI Development Rules

  ## General Rules

  - 本项目为新项目。实现新功能时，以当前需求和长期最优架构为准，不为旧代码、旧数据结构、旧交互或历史实现保留兼容层、迁移逻辑、降级路径或双轨方案；确认废弃后应直接移除旧路径。
  - 不要为了向后兼容而保留旧方案。应移除废弃路径，而不是新增兼容层、降级回退或迁移逻辑。
  - Always modify existing code instead of creating duplicate implementations.
  - Preserve current functionality.
  - Avoid unnecessary refactoring.
  - Keep changes minimal and focused.

  Before making major architectural changes:
  - Explain the reason.
  - Describe potential impact.

---

  # UI Development

  When implementing UI:

  - Follow the existing design system.
  - Use Tailwind CSS utilities.
  - Reuse existing components.
  - Maintain responsive behavior.

  Do not:
  - Add unnecessary CSS files.
  - Introduce another UI framework.
  - Change global styles without checking impact.

---

  # Code Quality

  All code must:

  - Use TypeScript.
  - Use React functional components.
  - Use default exports for components.
  - Keep components modular.
  - Avoid duplicated logic.

  Ensure:

  - JSX tags are correctly closed.
  - Types are explicitly defined when useful.
  - Build passes without errors.

  

  # API Architecture

  Current API layer:

  - Supabase Client SDK
  - Supabase Edge Functions when server-side logic is required


  Backend API should not be created unless:

  - Sensitive operations are required.
  - AI processing requires server execution.
  - Third-party API keys must be protected.


  Preferred backend technology:

  - TypeScript
  - Supabase Edge Functions
  - Deno runtime

---

  # Feature Development Process

  For every new feature:

  1. Analyze existing code.
  2. Identify affected files.
  3. Explain implementation approach.
  4. Modify only necessary files.
  5. Verify compatibility with existing features.

---

  # Dependency Rules

  Before installing new dependencies:

  - Check whether existing dependencies can solve the problem.
  - Explain why a new dependency is needed.

  Avoid unnecessary libraries.

---

  # Reference Projects

  ## Mandatory Module Research Workflow

  Before designing or implementing every new product module, follow this order strictly:

  1. Research whether mature public solutions, established browser APIs, or well-maintained libraries already address the module's core problem.
  2. Create an AisenLens implementation proposal based on that research, while keeping the existing stack and local-first product constraints.
  3. Inspect only module-relevant files in `reference-projects/`, prioritizing `opensource-openreel/` first and `opensource-opencut/` second.
  4. If either reference provides useful patterns, adapt and integrate those patterns into the proposal; do not copy its implementation wholesale.
  5. If neither reference provides useful value, proceed with the AisenLens proposal from step 2.
  6. Only after this decision is recorded may implementation begin.

  This workflow is mandatory for every future module. Do not skip the public-solution review or reverse the OpenReel → OpenCut reference priority.

  The `reference-projects/` directory contains three external reference projects:

  - `previous-aisenlens/`: previous AisenLens demo.
  - `opensource-opencut/`: open-source reference project.
  - `opensource-openreel/`: open-source reference project.

  Rules:

  - Do not read, copy, or migrate reference-project code proactively.
  - When implementing or designing a specific module, inspect only the files relevant to that module and purpose.
  - Use reference projects for mature patterns, data-model research, and interaction research; adapt findings to AisenLens rather than duplicating implementations.
  - After obtaining meaningful information from a reference project, update `reference-projects/REFERENCE_PROJECT_INDEX.md` with the module, files consulted, confirmed findings, and AisenLens decision.
  - Treat the index as verified notes only; do not record assumptions as confirmed facts.

---

  # Product Development Principle

  This is an evolving production application.

  Prioritize:

  1. Stability
  2. Maintainability
  3. User experience
  4. Performance

  The AI acts as a senior frontend engineer maintaining and improving an existing product.
