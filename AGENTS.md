## 架构规则

- 不要为了向后兼容而保留旧方案。应移除废弃路径，而不是新增兼容层、降级回退或迁移逻辑。
- 选择能完全满足当前需求的最简单实现。避免推测性的抽象、配置和间接层。
- 按层次逐步构建系统：先完成端到端可用的最小版本，再在已可用的产品上叠加每项新能力。不要为了未完成的复杂性牺牲可用产品。
- 保持组件模块化，并清晰分离关注点。
- 当成熟、维护良好的库能降低整体复杂度或提高可靠性时，优先使用它们。没有充分理由时，不要重复实现常见功能。
- 编写自定义实现或新增依赖前，先充分利用项目已有依赖。不要在未查看文档和类型定义前，就认定某个库缺少所需能力。
- 架构决策要面向长期。不要接受仅能暂时解决问题、且注定以后要替换的权宜之计。

## 工作方式

- 修改前先阅读相关代码、配置和现有约定。
- 变更保持小而聚焦；不要顺带重构无关模块或格式化无关文件。
- 优先复用项目现有依赖、组件、工具函数和代码模式。
- 不要覆盖、还原或删除用户已有的改动，除非用户明确要求。

## 代码质量

- 保持与周边代码一致的命名、结构、错误处理和类型约束。
- 新增行为应处理合理的空值、失败和边界情况。
- 注释只用于解释不直观的设计原因，不重复代码本身的含义。
- 不要引入未使用的依赖、文件、导入或死代码。

## 验证

- 完成修改后，运行与改动范围相称的检查、测试或构建。
- 若无法运行验证，明确说明未运行的命令和原因。
- 涉及界面改动时，检查桌面和移动尺寸下的布局、溢出与可用性。

## Git 与文件安全

- 不执行 `git reset --hard`、`git checkout --` 或其他会丢失用户改动的命令，除非得到明确授权。
- 不删除文件或构建产物，除非目标和范围已确认且操作属于当前任务。
- 提交前只暂存本次任务相关的文件；不要将用户的无关改动纳入提交。

## 沟通

- 每次沟通，要先称呼老板。
- 用中文简洁汇报完成内容、验证结果和仍存风险。
- 需求存在关键歧义且会显著影响实现时，先说明假设或请求确认。
